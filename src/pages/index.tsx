import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <h1 className="text-3xl font-semibold text-blue-600">Hello World !</h1>
    </div>
  );
};

export default Home;
