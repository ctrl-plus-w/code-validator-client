import { NextPage } from 'next';

import StudentLayout from '@layout/StudentLayout';

const Home: NextPage = () => {
  return (
    <StudentLayout>
      <h1>Building the page.</h1>
    </StudentLayout>
  );
};

export default Home;
